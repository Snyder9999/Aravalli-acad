export const DEFAULT_ROUTES_BY_ROLE = {
    admin: '/admin',
    user: '/',
    SUPER_ADMIN: '/admin',
    ADMIN: '/admin',
    USER: '/',
};

export const authenticationRoute = ['/login', '/register'];
